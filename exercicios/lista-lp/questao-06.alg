algoritmo "semnome"
// Função :Escreva um algoritmo para ler as dimensões de um retângulo (base e altura), calcular e escrever a
// área do retângulo.
var
base, altura, area : real
inicio
escreval("Digite a base")
leia(base)
escreval("Digite a altura")
leia(altura)
area := base * altura
escreval("Area total: ", area)
fimalgoritmo
