algoritmo "semnome"
// Escreva um algoritmo para ler um valor (do teclado) e escrever (na tela) o seu antecessor.
var
n : inteiro
inicio
// Seção de Comandos 
escreval("Digite 1 numero")
leia(n)
escreval("antecessor = ", n-1)
fimalgoritmo
